

## Change "How common is DLD?" heading color to black

**File:** `src/components/HowCommonSection.tsx`

Change the `<h2>` heading class from `text-primary` to `text-black` (or `text-foreground` to match the design system).

