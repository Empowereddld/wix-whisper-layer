

## Plan: Increase Hub Header Logo by 150%

**File: `src/components/hub/HubHeader.tsx` (line 50)**
- Change logo `className="h-8"` to `className="h-12"` (32px → 48px, exactly 150%)

One line, one file.

