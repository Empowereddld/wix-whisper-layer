

## Plan: Update "WHO WE SERVE" Dropdown

In `src/components/Header.tsx`, change the third item in `whoWeServeLinks` from `{ label: "For SLPs", href: "/for-slps" }` to `{ label: "For Therapists", href: "/for-therapists" }`.

Single line change, one file.

